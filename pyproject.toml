[project]
name = "sheet-music-viewer"
version = "0.1.0"
description = "anton's sheet music viewer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PyMuPDF>=1.26.0",
    "PyQt6>=6.8.0",
]

[project.scripts]
sheet-music-viewer = "sheet_music_viewer.app:main"
